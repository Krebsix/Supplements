# Kopie als .env anlegen (gitignored). Standard ist die produktive Edge
# Function (web/src/config.ts); fuer lokale Tests gegen
# `supabase functions serve beta-signup` die lokale URL eintragen.
# Leer lassen = Formular deaktiviert mit Hinweis "Anmeldung folgt".
# PUBLIC_BETA_SIGNUP_URL=http://127.0.0.1:54321/functions/v1/beta-signup
